https://takeuforward.org/plus/dsa/problems/highest-occurring-element-in-an-array?source=strivers-a2z-dsa-track
### Important - If multiple elements have the same maximum frequency, return the **smallest** one.
```python
nums = [1, 2, 2, 3, 3, 3]

# Step 1: Count frequencies
freq = {}

for num in nums:
    if num not in freq:
        freq[num] = 1
    else:
        freq[num] += 1

# Step 2: Find maximum frequency
maximum = max(freq.values())

# Step 3: Find the smallest element having maximum frequency
answer = float("inf")

for key in freq:
    if freq[key] == maximum:
        answer = min(answer, key)     #Important

print(answer)
```
If Question changed to this.........
**If multiple elements have the maximum frequency, return the largest element**
Then the following code is also correct.
```python
nums = [1, 2, 2, 1, 3]

freq = {}
for num in nums:
    if num not in freq:
        freq[num] = 1
    else:
        freq[num] += 1
maximum = max(freq.values())
for i in freq:
    if freq[i] == maximum:
        ans = i
print(ans)
```
