https://www.geeksforgeeks.org/problems/rotate-array-by-n-elements-1587115621/1
https://takeuforward.org/plus/dsa/problems/left-rotate-array?source=strivers-a2z-dsa-track
### TLE
### Test Cases Passed - 205 / 1115
```python
class Solution:
    def rotateArr(self, a, d):
        n = len(a)
        for _ in range(d):
            store = a[0]
            for i in range(1,n):
                a[i-1] = a[i]
            a[-1] = store
        return a
```
### Test Cases Passed - 327 / 1115
```python
class Solution:
    def rotateArr(self, a, d):
        n = len(a)
        d = d % n 
        for _ in range(d):
            store = a[0]
            for i in range(1,n):
                a[i-1] = a[i]
            a[-1] = store
        return a
```
**Understand why use modulo operator -**
Example - nums = [1,2,3,4,5,6,7] n = 7 k = 10 10 % 7 = 3

**Rotating 10 times is the same as:
7 rotations + 3 rotations**

After 7 rotations, the array comes back to its original state.
So only 3 rotations are actually needed.

Revision Line - 
Use k = k % n to remove complete rotation cycles because rotating an array by its length (n) returns it to the original position.

**But as we can see still it gives TLE like only 327 test cases passed out of 1115 so we need to optimize it more -**
```text
Lets use slicing
First understand this -
nums = [1,2,3,4,5,6,7]
k = 3
nums[-k:]   # [5,6,7]
nums[:-k]    # [1,2,3,4]
```
## Test Cases passed - 1115/1115
```
Input: arr[] = [1, 2, 3, 4, 5], k = 2
Output: [3, 4, 5, 1, 2]
```
## Submitted Code
```python
class Solution:
    def rotateArr(self, a, k):
        n = len(a)
        k = k%n
        a[:] = a[k:] + a[:k]      ## a[:] is not creating a copy here. It is being used on the left side of an assignment.
        return a

```
https://leetcode.com/problems/rotate-array/
```
Input: nums = [1,2,3,4,5,6,7], k = 3
Output: [5,6,7,1,2,3,4]
```
## Submitted Code on Leetcode - 
```python
class Solution(object):
    def rotate(self, nums, k):
        n = len(nums)
        k = k % n

        nums[:] = nums[-k:] + nums[:-k]
```

# Difference Between nums[] and nums[:]
```python
# Lets code to understand this

# 1. nums
# This is just a reference to the original list.

nums = [1,2,3]
a = nums

a[0] = 100

print(a)
print(nums)

# Both a and nums point to the same list in memory.
```
Output - 
``` 
[100, 2, 3]
[100, 2, 3]
```
```python
# 2. nums[:]
# This creates a shallow copy of the entire list.

nums = [1,2,3]
a = nums[:]

a[0] = 100

print(a)
print(nums)

# Here, a is a new list, so changes to a do not affect nums.
```
Output - 
```
[100, 2, 3]
[1, 2, 3]
```
```python
# Just to understand -
nums = [1,2,3,4,5,6,7]
k = 3

nums[-k:]      # [5,6,7]
nums[:-k]      # [1,2,3,4]

nums[-k:] + nums[:-k]    # [5,6,7,1,2,3,4]
```
Output - 
```
[5, 6, 7, 1, 2, 3, 4]
```
# Another Method to solve this question is -
The reverse (inverse) method is the standard true in-place solution for the Rotate Array problem.

Idea -

1. Reverse the entire array.
2. Reverse the first k elements.
3. Reverse the remaining n-k elements.

**Step 1:** Reverse whole array
```
[1,2,3,4,5,6,7]
↓
[7,6,5,4,3,2,1]
```
**Step 2:** Reverse first k=3 elements
```
[7,6,5,4,3,2,1]
↓
[5,6,7,4,3,2,1]
```
**Step 3:** Reverse remaining elements
```
[5,6,7,4,3,2,1]
↓
[5,6,7,1,2,3,4]
```
```python

def reverse(nums, left, right):
  while left < right:
    nums[left], nums[right] = nums[right], nums[left]
    left += 1
    right -= 1

def rotate(nums, k):
  n = len(nums)

  reverse(nums, 0, n-1)  # step 1 -reverse whole array
  reverse(nums, 0, k-1)  # step 2 - reverse first k elements
  reverse(nums, k, n-1)  # step 3 - reverse remaining
  return nums

rotate([1,2,3,4,5,6,7],3)
```
