https://takeuforward.org/plus/dsa/problems/second-largest-element?source=strivers-a2z-dsa-track
https://www.geeksforgeeks.org/problems/second-largest3735/1
# Second Largest Element
### Method -1
```python
class Solution:
    def getSecondLargest(self, arr):
        # code here
        max_1 = -1         #float("-inf")
        max_2 = -1         #float("-inf")
        for num in arr:
            if num > max_1:
                max_2 = max_1
                max_1 = num
            elif num == max_1:   #Important for example - arr= [10,5,10] 
                continue
            else:
                max_2 = max(max_2, num)
        return max_2
```
### or
```python
class Solution:
    def getSecondLargest(self, arr):
        max_1 = float("-inf")   # max_1 = -1
        max_2 = float("-inf")   # max_2 = -1

        for num in arr:
            if num > max_1:
                max_2 = max_1
                max_1 = num

            elif num != max_1:
                max_2 = max(max_2, num)

        return max_2
```
### Method -2
# Second Largest and Second Smallest Element
**This Approach can easily be used for finding third Largest/Smallest element and so on.** by adding another for loops.
- for input = [5,5,5] expected output is -1 so we can submit this code according to the test case
- **but if - for input = [5,5,5] if expected output would be 5 then we have to use method 1**
```python
class Solution:
    def getSecondLargest(self, arr):
        # code here
        max_1 = -1       # float("-inf")  
        max_2 = -1       # float("-inf")  
        min_1 = float("inf")
        min_2 = float("inf")
        for i in range(len(arr)):
            min_1 = min(min_1, arr[i])
            max_1 = max(max_1, arr[i])
        for i in range(len(arr)):
            if arr[i] > max_2 and arr[i] != max_1:
                max_2 = arr[i]
            if arr[i] < min_2 and arr[i] != min_1:
                min_2 = arr[i]
        return max_2
```
# Third largest Element
### Method - 1   Submitted code - 
```python
class Solution:
    def thirdLargest(self,arr):
        max_1 = -1 #float("-inf")
        max_2 = -1 #float("-inf")
        max_3 = -1 #float("-inf")

        for num in arr:
            if num > max_1:
                max_3 = max_2
                max_2 = max_1
                max_1 = num

            elif num > max_2:
                max_3 = max_2
                max_2 = num

            elif num > max_3:
                max_3 = num
        return max_3
```
### Method - 2  can't Submit
according to the test cases for input = [5,5,5] expected output = 5
Can't submit because for arr = [5,5,5] expected output  = 5 but my output  = -1 so, we can only submit method 1 here
```python
class Solution:
    def thirdLargest(self,arr):
        # code here
        max_1 = -1         
        max_2 = -1    
        max_3 = -1
        min_1 = float("inf")
        min_2 = float("inf")
        min_3 = float("inf")
        for i in range(len(arr)):
            min_1 = min(min_1, arr[i])
            max_1 = max(max_1, arr[i])
        for i in range(len(arr)):
            if arr[i] > max_2 and arr[i] != max_1:
                max_2 = arr[i]
            if arr[i] < min_2 and arr[i] != min_1:
                min_2 = arr[i]
        for i in range(len(arr)):
            if arr[i] > max_3 and arr[i]!= max_1 and arr[i] != max_2:
                max_3 = arr[i]
            if arr[i] < min_3 and arr[i]!= min_1 and arr[i] != min_2:
                min_3 = arr[i]
        return max_3
```
