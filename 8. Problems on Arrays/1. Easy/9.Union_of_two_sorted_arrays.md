https://takeuforward.org/plus/dsa/problems/union-of-two-sorted-arrays?source=strivers-a2z-dsa-track
https://www.geeksforgeeks.org/problems/union-of-two-sorted-arrays-1587115621/1
# cant sumit..try again for duplicates
```python
class Solution:
    def findUnion(self, a, b):
        n1 = len(a)
        n2 = len(b)
        i = 0
        j = 0
        c = []
        while i < n1 and j < n2:
            if a[i] < b[j]:
                c.append(a[i])
                i += 1
            elif b[j] < a[i]:
                c.append(b[j])
                j += 1
            else:  #a[i] == b[j]
                c.append(a[i])
                i += 1
                j += 1
        while i < n1:
            c.append(a[i])
            i += 1
        while j < n2:
            c.apend(b[j])
            j += 1
        return c

```
