gfg - https://www.geeksforgeeks.org/problems/reverse-digit0316/1

tuf - https://takeuforward.org/plus/dsa/problems/reverse-a-number?source=strivers-a2z-dsa-track
## TLE
```python
n = 4123
while n > 0:
    digit = n % 10
    while digit >= 0:
        print(digit, end = "")
        break
    n = n // 10
```
```python
class Solution:
	def reverseDigits(self, n):
		while n > 0:
		    digit = n % 10
		    while digit >= 0:
		        print(digit, end = "")
		    n = n // 10
```
### Theory - 
`reverse = reverse * 10 + last_digit`

Used to **append the extracted digit to the end of the reversed number**.

###### Formula

```python
reverse = reverse * 10 + last_digit
```
# Submitted Code - 
```python
class Solution:
	def reverseDigits(self, n):
	    reverse = 0
		while n > 0:
		    last_digit = n % 10
		    reverse = reverse*10 + last_digit
		    n = n // 10
		return reverse
```
		    
