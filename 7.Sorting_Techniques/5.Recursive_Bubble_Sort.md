https://takeuforward.org/plus/dsa/problems/recursive-bubble-sort?source=strivers-a2z-dsa-track
```python
def bubble_sort(arr, n):
    # Base case: if only 1 element, return
    if n == 1:
        return

    # Perform one pass to push the largest to the end
    for j in range(n - 1):
        if arr[j] > arr[j + 1]:
            # Swap elements
            arr[j], arr[j + 1] = arr[j + 1], arr[j]

    # Recurse for the rest of the array
    bubble_sort(arr, n - 1)


# Main driver
arr = [13, 46, 24, 52, 20, 9]
n = len(arr)

print("Before Using Bubble Sort:")
print(arr)

bubble_sort(arr, n)

print("After Using Bubble Sort:")
print(arr)
```
| Comparision              | Recursive              | Iterative       |
| --------------------- | ---------------------- | --------------- |
| Correct               | ✅                     | ✅               |
| Time                  | O(n²)                  | O(n²)           |
| Extra space           | O(n) due to call stack | O(1)            |
| Easy to understand    | Medium                 | **Easy**        |
| Common implementation | Less common            | **Very common** |
| Interview             | Can be discussed       | **Preferred**   |
