https://www.geeksforgeeks.org/problems/reverse-digit0316/1
```python
class Solution:
	def reverseDigits(self, n):
	    reverse = 0
		while n > 0:
		    last_digit = n % 10
		    reverse = reverse*10 + last_digit
		    n //= 10
		return reverse
```
https://leetcode.com/problems/reverse-integer/
```python
class Solution:
    def reverse(self, n):
        reverse = 0
        if n < 0:
            n = abs(n)
            while n > 0:
                last_digit = n % 10
                reverse = reverse*10 + last_digit
                n //= 10
            return -1*reverse               #Important
        else:
            while n > 0:
                last_digit = n % 10
                reverse = reverse*10 + last_digit
                n //= 10
            return reverse
```
### Method 2
```python
class Solution:
    def reverse(self, n):
        sign = -1 if n < 0 else 1
        n = abs(n)
        reverse = 0
        while n > 0:
            last_digit = n % 10
            reverse = reverse*10 + last_digit
            n //= 10
        return sign * reverse
```
**According to constraints - Submitted solution is -**
```python
class Solution:
    def reverse(self, n):
        sign = -1 if n < 0 else 1
        n = abs(n)
        reverse = 0
        while n > 0:
            last_digit = n % 10
            reverse = reverse*10 + last_digit
            n //= 10
        
        reverse =  sign * reverse
        if reverse < -2**31 or reverse > 2**31 - 1:     #if reverse >= -2**31 and reverse <= 2**31 - 1:
            return 0                                              #return reverse

        return reverse                                   # return 0
```
