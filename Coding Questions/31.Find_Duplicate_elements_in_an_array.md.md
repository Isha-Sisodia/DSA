https://www.geeksforgeeks.org/problems/find-duplicates-in-an-array/1
```python
class Solution:
    def findDuplicates(self, arr):
        result = []
        seen = {}
        for num in arr:
            if num in seen:
                result.append(num)
            else:
                seen[num] = 1
        return result
```
