https://www.geeksforgeeks.org/problems/remove-duplicates-in-small-prime-array/1
```python
class Solution:
    def remDuplicate(self, arr):
        freq = {}
        result = []
        for num in arr:
            if num in freq:
                freq[num] += 1  # do nothing
            else:
                freq[num] = 1
                result.append(num)
        return result
```
